[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "auth-service"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["securebank-shared @ file:///app/shared"]

[tool.setuptools.packages.find]
where = ["."]
